// BUCLES FOR  
//los bucles for sirven para cuando sabemos exactamente la cantidad de veces que queremos
//que se ejecute el codigo
//siempre requiere de una variable entera que cumpla la funcion de contador

//ej:
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
 int main(){
    //int i = 1123;
    
    for (int i = 1; i <= 100; i++) //aca siempre ; nunca , 
    {
        printf("%d-", i);
    }
    //este es un contador del 1 al 100
    return 0;
}

//Este es un ej de bucle infinito porque la condicion nunca va a ser falsa:
int main() {

    for (int i = 1; i < 100; i--) //esto es debido a que i siempre va a ser menor a 100 
    {
        printf("%d - ", i);
    }
    return 0;
}
*/

